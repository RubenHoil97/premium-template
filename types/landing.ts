export type Service = {
  title: string;
  description: string;
};

export type Testimonial = {
  name: string;
  text: string;
};

export type Benefit = string;

export type Faqc = {
  question: string;
  answer: string;
};

export type Hero = {
  badge: string;
  title: string;
  description: string;
  primaryCta: string;
  secondaryCta: string;
};

export type Theme = {
  background: string;
  text: string;
  mutedText: string;
  card: string;
  cardHover: string;
  border: string;
  badge: string;
  accent: string;
  primaryButton: string;
  secondaryButton: string;
  glow: string;
  gradient: string;
};
